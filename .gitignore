# 개인 config (원칙상 ~/.claude/kiki/ 에 두지만 repo 내 실수 방지 안전망)
*.config.json
!*.config.example.json

# credential — 절대 커밋 금지
*token*
*.key
.env
.env.*
*.env
!*.env.example
dooray_token.txt

# 로컬 전용 유지보수 노트 (배포 X — 메인테이너 세션 온보딩용, repo 에 올리지 않음)
# ⚠️ 배포용 CLAUDE.md(= 사용자/에이전트 설치·실행 지침)는 추적함. 메인테이너 노트만 .local 로 분리해 제외.
CLAUDE.local.md

# 임시 작업 파일 (조회 캐시·테스트 스크립트)
_tmp/

# OS / 편집기
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# 로고 분해 자산(4k components)은 repo에 올리지 않음 (용량 크고 불필요 — 로컬 보관)
assets/components/
