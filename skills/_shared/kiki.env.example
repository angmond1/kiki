# kiki 공통 토큰 — 모든 kk-* skill 이 공유한다.
# 이 파일을 ~/.claude/kiki/kiki.env 로 복사 (repo 밖, gitignore). 값은 본인 로컬에만.
#
# Dooray 개인 인증 토큰 — 발급: https://kist.gov-dooray.com/setting/api/token
#   (Dooray Web → 개인설정 → API → 개인 인증 토큰)
# 필요 skill: kk-rpa(드라이브 업로드) / kk-dining(RPA 업로드 옵션).
# 조회 전용 skill(kk-budget·kk-inspect)·세션쿠키 skill(kk-mail)은 토큰 불요.
DOORAY_TOKEN=
