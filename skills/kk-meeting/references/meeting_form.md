# 회의록 양식(hwpx 옵션)·인원·회의시간·증빙·중복방지

> 2026-06-05~ **회의록 master = 엑셀** (→ `meeting_log_excel.md`). 본 파일의 hwpx 별지1호 양식은 **사용자가 요청할 때만** 사용(완료 보고 때 묻는다, 2026-09-24~ 자동 생성 안 함) — **아래아한글 없이 생성**(`scripts/make_dininglog_hwpx.py`).
> fam_0704 지급신청서 직접 자동작성 워크플로(권장)에서는 hwp 양산 자체가 불필요 (엑셀이 master + fam_0704 입력에 그대로 사용). → `fam_0704_automation.md`.

## (옵션) hwpx 별지1호 양식 = 회의비 사용내역서(회의록), 7x7 표 1개
`scripts/make_dininglog_hwpx.py` 가 `assets/minutes_template.hwpx`(zip) 의 `Contents/section0.xml` 에서 표의 `<hp:tc>` 를 문서 순서로 세어 **값 셀만 텍스트 치환**한다(한글 불요, 모든 OS, 표준 라이브러리). **값 셀 인덱스**(구 pyhwpx `TableRightCell()` 순회 순서와 동일):

| 셀 | 값(data 키) | 셀 | 값(data 키) |
|:--:|------|:--:|------|
| 1 | account 계정번호 | 13 | time 회의시간 |
| 3 | pi_ins 연구책임자(+" (인)") | 15 | content 회의내용 |
| 5 | amount 금액(예 `104,200 원`) | 18 | ext_cnt 외부인원 |
| 7 | place 회의장소 | 19 | ext_mem 외부명단 `(소속) 성명` |
| 9 | date 회의일자(예 `2026. 5. 21.`) | 21 | int_cnt 내부인원 |
| 11 | purpose 회의목적(=회의록 제목) | 22 | int_mem 내부명단 `성명, 성명` |

라벨 셀(0,2,4,6,8,10,12,14,16,17,20)은 **건드리지 않음** → 원본 양식 100% 보존.
- 셀 채우기: 셀의 첫 단락(`<hp:p>`)을 원형으로 복제해 **줄마다 단락 1개**(`\n` = 새 단락, 빈 줄 = 빈 run), 첫 run 의 글자모양(charPrIDRef) 유지, `& < >` 는 XML 이스케이프, `Preview/PrvText.txt` 도 갱신. 한글 2014+·HOP 에서 열림 — 2026-09-23 한글 COM 라운드트립(재저장 후 셀 매핑 동일)·PDF 내보내기로 검증. 디버그: `python make_dininglog_hwpx.py --dump <파일>`. (구형 한글 COM 방식은 `hwp_automation.md`, 미사용)
- ⚠️ **템플릿의 `<hp:linesegarray>`(줄 배치 캐시)는 복제하지 않는다** — 남기면 한글이 그 줄 폭에 맞춰 자간을 눌러 한 줄에 우겨 넣는다(31자 목적이 눌린 원인, 2026-09-24 실측·수정). 생성기가 제거하므로 긴 목적·내용도 정상 줄바꿈. 필요 시 `_fill_cell(cell, text, para_pr=, char_pr=)` 로 문단·글자모양 ID 를 지정(양식 힌트의 빨간 글자·가운데 정렬 회피). **HOP 0.4.4 에서 hwpx 표시 확인**(2026-09-24, 사용자 확인).

## 인원 산정 (2026-06-05 정정)
- **필요 인원 = ⌈금액(식대+음료 합산) ÷ 50,000⌉ + 1명** (규정 **1인 5만원 한도 = 식대+음료(액체 다과) 합산** + 여유 1명) — **kk-meeting 표준 산정식**.
- ⭐ **같은날 식당+카페 = 합산 금액**으로 산정 (옛 "카페 음료 잔수=인원" 폐기 — 식당+카페 따로 보지 않고 합산). 예: 식당 70,000 + 카페 28,300 → 합산 98,300 → ⌈1.966⌉+1 = 3명.
- 카페 영수증 음료 수는 증빙 일치 참고만 (인원 산정 기준 아님).
- 내부/외부 배분은 사용자 지정. 외부=`(소속) 성명`, 내부=성명만.

## 회의시간 융통성 (2026-06-05 신규)
- **회의 종료시간 = 카드승인시간(`ds_datagrid1.USETIME`) 참고 융통성 설정**. 결제는 회의 직후 → **종료 ≤ 결제**.
- 예: USETIME 14:38 → 회의 13:00~**14:30** (결제 직전 정각/반정각으로 자연스럽게).
- 30분/시간 단위 반올림. 회의 시작은 1시간/1시간 30분 전 일반적.

## 회의비 판별 + 증빙(영수증) — 재무팀 매뉴얼(2026.06) 기준
- 회의비 = **음식점·카페 카드결제**. 같은 날 식사+카페 = 1건(금액 합산).
- 기본은 **카드매출전표로 갈음**(별도 영수증 불필요). 단 **업종별 추가 증빙** 필요:

  | 업종 | 추가 증빙 |
  |------|----------|
  | 편의점·제과점·슈퍼마켓·대형마트 | 거래명세서 |
  | 백화점 푸드코트·호텔·리조트 | 식당명 기재 영수증 |
  | 인터넷 배달(배민 등) | 배달장소 포함 세부내역 |
  | (청정센터 보충) 카페 외 슈퍼·베이커리·삼성코인밴딩(L3매점)·글로벌지식협력단지 | 내역 영수증 |

- ⭐ **카페 판별(보수적)**: **명확한 커피전문점**(스타벅스·테라로사·투썸플레이스·커피빈 등) → 카드전표 갈음(영수증 불요). **업종이 카페인지 애매하면 보수적으로 첨부**. ⚠️ **제과점+카페 겸업**(파리바게트·던킨도너츠·뚜레쥬르 등)은 **제과점 분류 → 거래명세서 첨부**(특히 주의).
- ⚠️ 명확한 카페·일반 식당은 카드전표로 충분(옛 "카페 무조건 첨부"는 부정확). 위 업종 해당 또는 카페 여부 애매 시에만 사용자에게 영수증 요청.
- ⭐ **영수증 파일명 표준 (2026-06-05 정정)**: **`{yymmdd}_{거래처명}.jpg`** (결제일 yymmdd 기준, 예 `260430_파리바게뜨.jpg`).
- ⚠️ **반드시 jpg 변환 필수** — `jpeg`/`png`/`pdf` 등은 행정원 시스템에서 안 받음. 사진 jpg 면 그대로, 다른 확장자면 변환.
- (옵션 = hwpx 동봉) 옛 방식: 회의록 hwpx 와 **동일 prefix + `_영수증(상호).jpg`**. rename 은 `]` 때문에 python `os.rename`.

## 회의비 규정 (집행 한도·금지) — 매뉴얼
- **1인 5만원 한도**(식대+다과). 메뉴 최저가 5만원 초과 음식점 이용 불가(예 63뷔페).
- **50만원 초과**: 일상감사 + 차상위자 결재(상한 소/본부장), 분할결제 유의.
- **주류·유흥성 불가**(주점·이자카야 등 실주점 포함), **심야 23~06시 불가**, 과제와 직접 관련성 필수(체육대회 답사·행사준비 등 X).
- 휴일·근무지 외(소속 시·군 또는 12km 미만 밖) 사용: **차상위자 전결 사전내부결재** 필요.
- 출장 중 사용: 출장 식비 **공제 확인**(중복 불가, 미반영 시 1식 반납+수입확인서). 출장비와 회의비 계정 다르면 공동계정 사유 기재.

## 사전 내부결재 + 장소·다과 (통합정보 회의비 화면 안내)
정산과제 회의비 식비는 **외부 인원 참석 + 사전 내부결재**가 원칙(계정별 가능 여부는 `project_code.md`). 사전 내부결재:
- **요건**: 전자결재 승인 + 회의 목적·일시·장소·내용·참석자 수(명단) 포함.
- **시점**: '회의 시작' 또는 '식비 실제 사용(카드결제) 시점' 중 **늦은 때까지 완료**(사전 구매·결제는 그 전까지).
- **수정**: 사전결재 내용(목적·일시·장소·참석자)이 바뀌면 → ① **회의록에 변경 반영** 또는 ② 내부결재 사후수정 (사전·사후 결재문서 함께 첨부).
- ⭐ **장소 처리**(회의록 작성 핵심): 장소가 미리 정해짐 → **식당명 기재**. 안 정해짐 → **회의실로 작성 후, '회의실 → 식당명' 변경을 회의록에 기입**해 증빙.
- **다과 구분**: 생수·커피·음료(액체류) = 사전결재 없으면 **영수증 첨부**로 가능. 과자·빵·쿠키·믹스커피(캡슐)·일반차 = **사전결재 필수**.
- 사전 내부결재문서 양식: 통합정보 → 경영정보 → 재무 → 회의비 내부결재 → 작성 → 첨부.

## 회의내용 (10만원 기준)
- **10만원 미만 = 회의내용 생략 가능 / 10만원 이상 = 작성 필수.**
- 무작위 금지 — 회의목적 + 해당 **과제보고서** 테마를 참고해 작성. 항목당 4 하위불릿 정도, **1.·2. 항목 사이 빈 줄 1개**(content 에 `\n` 빈줄).
- ⚠️⚠️ **괄호 `()` 절대 금지** — 괄호를 쓰면 AI 작성 티가 난다. 약어·부연설명은 괄호 없이 풀어쓰거나 쉼표로 처리. `(전기화학적 CO2 포집·전환)` ✗ → `전기화학적 CO2 포집·전환 기술` ✓ / `기술성숙도(TRL)` ✗ → `기술성숙도 수준` ✓ / `주요 생산물(CO·포름산·에틸렌 등)` ✗ → `CO·포름산·에틸렌 등 주요 생산물` ✓. 회의내용은 괄호 0개로 작성.
- ⭐ **중복 금지**: 과거 회의록(아래 폴더)을 작업마다 1회 스캔 → **제목·회의내용 모두 이전 것과 다르게**(같은 제목/내용 재사용 금지).

## 저장 경로 + 파일명

### 엑셀 (기본, 모든 모드)
- 위치: `{kiki_root}\dining\meeting_log\` **한 폴더**(하위 폴더 없음).
- 파일명: **`{yymm}_회의록.xlsx`** (yymm = 지급신청 처리 연월). **그 달 처리 건 = 동일 엑셀에 행 추가**. 주된 목적은 중복 방지 기록.
- 9컬럼 형식: → `meeting_log_excel.md`

### (요청 시) hwpx — 사용자가 원할 때만
- 출력: 엑셀과 **같은** `{kiki_root}\dining\meeting_log\` 폴더.
- 파일명: `{yymmdd}_{과제번호}_{과제이름 아주 간략히}_회의록.hwpx` (예 `260924_26E0001_e-Chemical_회의록.hwpx`; hwpx 는 회의록 1건만 담으므로 건당 1파일). 생성: `make_dininglog_hwpx.make_batch([{'data': {...}, 'hwpx': 경로}])` — 한글 불요. **비목 = 계정 분류코드별**: 일반 `33_523`, 수탁·소액·자체(S·I·F·B·K·G) `17_448` (→ `project_code.md`).

## 해외 회의비 (2026-09-07 실증, 재무팀 매뉴얼·해외출장 세부기준)
- 참고: 인사경영팀 「1-3. 해외출장 FAQ」(2026-08-25 신설, `wiki/3538560283559420253/4341540776265539338`) — 출장 식비·숙박 정산(호텔 조식 포함 시 식비, 기내 식사 공제, 공동숙박 등)·항공권 **과제카드 결제 시 카드 결제일 전 지급신청** 규칙. 회의비로 쓴 식사는 출장식비 공제·반납 여부 확인.
- **국내와 다른 점**: ① **영수증 첨부 필수**(카드전표 갈음 불가 — 해외출장 세부기준 회의비 제출서류: 법인/연구비카드 영수증·회의록·카드사용내역서) ② **카드사용내역서**(법인카드 해외이용내역, 적용환율 증빙) ③ **해외출장신청서** 첨부(식비 공제 확인, ⭐ **증빙 영역 fileDiv2** 에 — 사전결재문서 fileDiv3 아님) ④ 출장 식비를 이미 받은 날이면 **1식 반납 후 수입의뢰서** 건별 첨부.
- 금액 = fam_0711 `USEAMT` 확정 원화(임의 환산 금지). **1인 5만원 한도는 한화 기준**(재무팀 FAQ). 회의시간은 **현지시간 그대로**. 출장계정≠회의비계정이면 적요에 공동계정 사용 사유.
- ⭐ **해외 가맹점 = 거래처구분 콤보에서 "거래처명" 선택** (2026-09-07 사용자 확정): 해외 카드건은 가맹점번호(국내 사업자)가 없어 `CUSTCD` 가 비고, fam_0704 `bt_save` 검증 `CUSTCLSCD!='2' && (CUSTCD||CUSTNM 빈)` 에 걸려 "N번째 신청내역의 거래처 관련 항목을 입력해 주시기 바랍니다". → 거래처구분을 **"거래처명"**(거래처코드 없이 거래처명만 쓰는 구분. 검증식상 CUSTCD 면제 코드는 '2' — 첫 실행 때 콤보 innerdataset 라벨로 '2'=거래처명인지 확인)으로 바꾸고 거래처명 입력: 행별 `doGetDesp()` → `combo_custcls.set_value(코드)` + `F.switch1_RAWCARD_combo_custcls_onitemchanged.call(F,cb,{postvalue:코드,prevalue:'3'})` + `formDetail_Custnm.set_value(거래처명)`+`_onkillfocus` + `ds_rqstGrid.setColumn(i,'CUSTCLSCD',코드)`/`'CUSTNM'` 후 통장표기 재동기화 → 저장 통과. 국내 카드는 기본 '3'(가맹점 자동매핑) 그대로.
- **2026-08-01~ 사전결재 폐지**(국내·해외 모두). 대신 **외부참석자 참여연구원여부(`PROJJOINYN`) 필수**.

## ⭐ 2026-08-01 개정 — 통합정보 "회의비 집행 예산항목 / 국가연구개발사업 회의비 중 식비 사용기준" 안내 팝업 (fam_0704 신규 시 식비안내 팝업 원문, 2026-09-08)
- **회의비 집행 예산항목**: 연구비 `33-523`(**K과제 34-448**), `17-448`(**G과제 17-523**), `33-185`(연구실운영비) / 경상비 `20-440`(매식비) `28-526`(업무추진비) `37-531`(기관업무추진비) `38-511`(연구개발비) `68-440`(기본조사연구비) `42-451` `32-453`(행사비) `30-512`(교육훈련비) `16-442·443·447`(보조비).
- **사전 내부결재 절차 폐지 — 대상 한정**: 과학기술정보통신부·산업통상자원부·문화체육관광부·식품의약품안전처·국방부·환경부·보건복지부·경찰청·기상청 과제 + **주요사업과제**(E 등). ⚠️ **위에 없는 기타부처 사업은 아직 사전결재 필요**(소관 부처 규정 개정에 따라 단계적 폐지 예정, 수탁사업운영팀 확인) → 과제 소관부처를 먼저 확인(예: 주요사업(E) 과제 = 폐지 대상).
- **모든 정산과제는 "과제 미참여자" 참석 필수** (재무팀 지급신청 매뉴얼 2026-09-15 개정에도 명문화: "사전내부결재 폐지 — 26.8.1 사용분부터, 일부 부처 과제는 순차 적용" + "**타 기관 소속 참여연구원은 내부참석자로 간주**" → 외부 참여연구원은 미참여자 요건을 못 채움) — 회의비 집행 가능 사례(KIST 참여연구원 / KIST 미참여자 / 외부 참여연구원 / 외부 미참여자):
  | 사례 | KIST 참여 | KIST 미참여 | 외부 참여연구원 | 외부 미참여 | 집행 |
  |---|---|---|---|---|---|
  | 1 | O | O | X | X | **O** |
  | 2 | O | X | O | X | **X** (외부가 전부 참여연구원이면 불가) |
  | 3 | O | X | X | O | **O** |
  | 4 | O | O | X | O | **O** |
  → **외부참석자 `PROJJOINYN` 이 전부 'Y' 면 집행 불가**. 최소 1명은 미참여자(N)여야 함. **KIST 소속 과제 미참여자 + 외부기관 인원 전부 = "외부" 참석자 grid 에 기재**(KIST 미참여자는 회사명 한국과학기술연구원). 과제 참여연구원 범위는 연구개발실이 추후 확정.
- **사전 내부결재(기타부처 과제만)**: 전자결재 승인 + 미참여자 참석 회의만 식비 허용(해외출장 사용 시 동일). 시점 = 회의 시작 또는 카드 결제 중 늦은 때까지(사전 구매·결제는 그 전). **사전결재 내용(목적·일시·장소·내용·참석자 수) 변경 시 → 회의록 내용란에 변경 사유 기재**.
- **다과**: 생수·커피·음료(액체류) = 사전결재 없이 집행 시 **영수증 첨부** / 과자·빵·쿠키·믹스커피(캡슐)·일반차 = **사전내부결재 필요**.
- ⚠️⚠️ **이 안내 팝업은 하단 "확인"을 눌러야 사라진다** — `bt_close_onclick` 호출 후 **반드시 `imp_pop_fam_intro.visible===false` 검증**, 안 닫히면 팝업 form 의 확인 버튼 핸들러 재호출 또는 좌표 클릭(2026-09-08 사용자 지적: 호출만 하고 검증 안 해 팝업이 남아 사용자가 직접 닫음). **다음부터 반드시 닫고 검증할 것.**
